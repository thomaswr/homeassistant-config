default_view:
  view: yes
  entities:
    - group.weather
    - group.lights
    - media_player.chromie_audio
    - media_player.chromie
    - media_player.kodi
    - media_player.onkyo
    - group.all_scenes

persons:
  view: yes
  icon: mdi:account-multiple
  entities:
    - device_tracker.thomas_nexus6p
    - device_tracker.p8lite
    - camera.thomas
    - camera.nexus7cam

monitor:
  view: yes
  icon: mdi:monitor-multiple
  entities:
     - group.computers
     - group.mediadevices
     - group.networkdevices

server:
  view: yes
  icon: mdi:server
  entities:
     - group.transmission

system:
  view: yes
  icon: mdi:settings
  entities:
    - group.systeminfo
    - group.all_lights
    - group.all_switches
    - group.all_automations
    - group.all_scripts

