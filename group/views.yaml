default_view:
  view: yes
  entities:
    - group.weather
    - history_graph.temperature
    - group.climate
    - group.heating
    - group.lights
    - group.mediaplayers

persons:
  view: yes
  icon: mdi:account-multiple
  entities:
    - group.tracker
    - camera.thomas
    - group.modes

monitor:
  view: yes
  icon: mdi:monitor-multiple
  entities:
     - group.computers
     - group.mediadevices
     - group.networkdevices

server:
  view: yes
  icon: mdi:server
  entities:
     - sensor.speedtest
     - group.transmission

system:
  view: yes
  icon: mdi:settings
  entities:
    - group.systeminfo

