###################################
##  Start Up Section
###################################

- alias: Startup Notification
  trigger:
    - platform: homeassistant
      # Event can also be 'shutdown'
      event: start
  action:
    service: notify.notify
    data_template:
      message: "Home Assistant gestartet"