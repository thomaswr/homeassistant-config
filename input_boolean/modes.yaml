guest_mode:
  name: Gastmodus
  icon: mdi:sofa
  initial: off

vacation_mode:
  name: Urlaubsmodus
  icon: mdi:wallet-travel
  initial: off

heating_mode:
  name: Heizung
  icon: mdi:radiator
  initial: >
          {% if now().month <= 3 and now().month >= 10 %}
            on
          {% else %}
            off
          {% endif %}